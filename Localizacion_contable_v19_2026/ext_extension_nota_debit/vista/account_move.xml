<odoo>
  <data>

     <record id="resumen_account_view_form22_nc" model="ir.ui.view">
        <field name="name">resumen.account.view.form22_nc</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_date']" position="after">
                  <!--field name="date" string="Fecha odoo." invisible="move_type in ('in_invoice','out_invoice')" /-->
                  <field name="date_nota_credito" string="Fecha Nota Credito"  invisible="1"/>
                  <field name="date_fact_afectada" string="Fecha Factura Afectada"  invisible="1"/>
            </xpath>

            <!--xpath expr="//button[@name='action_reverse']" position="after">
                <button name="791" string="*Generar Nota de debito*" type="action" groups="account.group_account_invoice" attrs="{'invisible': ['|', '|', ('debit_origin_id', '!=', False),('move_type', 'not in', ('out_invoice', 'in_invoice', 'out_refund', 'in_refund')), ('state', '!=', 'posted')]}"/>
            </xpath-->
        </field>
    </record>     	 

  </data>
</odoo>